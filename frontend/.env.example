# Backend API base URL.
# For local development with Vite proxy (recommended): leave empty or comment out.
# Vite will proxy /vocab/* requests to the backend automatically.
# Only set this when running the built frontend against a remote backend.
# VITE_API_URL=http://127.0.0.1:8000

